{% extends 'base.html.twig' %}

{% block title %}FolkDistrict{% endblock %}

{% block body %}
    <h1>FolkDistrict</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ folk_district.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ folk_district.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_folk_district_index') }}">back to list</a>

    <a href="{{ path('app_folk_district_edit', {'id': folk_district.id}) }}">edit</a>

    {{ include('folk_district/_delete_form.html.twig') }}
{% endblock %}
