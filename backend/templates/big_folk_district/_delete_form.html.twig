<form method="post" action="{{ path('app_big_folk_district_delete', {'id': big_folk_district.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ big_folk_district.id) }}">
    <button class="btn">Delete</button>
</form>
