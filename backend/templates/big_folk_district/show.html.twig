{% extends 'base.html.twig' %}

{% block title %}BigFolkDistrict{% endblock %}

{% block body %}
    <h1>BigFolkDistrict</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ big_folk_district.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_big_folk_district_index') }}">back to list</a>

    <a href="{{ path('app_big_folk_district_edit', {'id': big_folk_district.id}) }}">edit</a>

    {{ include('big_folk_district/_delete_form.html.twig') }}
{% endblock %}
